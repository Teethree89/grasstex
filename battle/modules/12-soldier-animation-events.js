/* Bridges gameplay fire/reload events to semantic soldier animation tags. */
(function(root){
  'use strict';
  if(!root.BattleModules||!root.BattleSoldierModel||!root.SquadAI||!root.BattleSim)return;
  var oldStart=root.BattleSim.start,oldUpdate=root.SquadAI.updateSoldier,T=root.BattleSoldierModel.TAGS;
  function startReload(s,battle){if(!s||s.dead||!s.weapon||s.reloading)return;var dur=s.weapon.stats.reloadTime||2.5;s.reloading=true;s.reloadStart=battle.time;s.reloadUntil=battle.time+dur;s.fireCooldown=Math.max(s.fireCooldown||0,dur);root.BattleSoldierModel.triggerAnimation(s,T.reload,{weapon:s.weapon.kind,duration:dur});if(root.BattleTelemetry)root.BattleTelemetry.record('decision-reload',{soldier:s.id,faction:s.faction,weapon:s.weapon.kind,duration:dur},battle);}
  function finishReload(s){if(!s||!s.weapon)return;s.reloading=false;s.weapon.ammo=s.weapon.magSize||s.weapon.stats.magazine||8;s.reloadStart=0;s.reloadUntil=0;}
  root.SquadAI.updateSoldier=function(s,battle){if(s&&!s.dead&&s.weapon){if(s.reloading&&battle.time>=s.reloadUntil)finishReload(s);if(!s.reloading&&s.weapon.ammo<=0)startReload(s,battle);if(s.reloading)s.fireCooldown=Math.max(s.fireCooldown||0,Math.max(.16,s.reloadUntil-battle.time));}oldUpdate(s,battle);if(s&&s.reloading){s.setUp=false;s.tacticalCrouch=true;if(s.target&&root.BattleMovementResolver){root.BattleMovementResolver.proposeCombat(s,{x:s.root.position.x,z:s.root.position.z},battle,'reload-hold');root.BattleMovementResolver.resolve(s,battle);}else if(s.target)s.destination={x:s.root.position.x,z:s.root.position.z};}};
  root.BattleSim.start=function(scene,opts){var sim=oldStart(scene,opts),oldFire=sim.onFire;sim.onFire=function(soldier){if(soldier&&soldier.weapon){soldier.weapon.ammo=Math.max(0,(soldier.weapon.ammo==null?soldier.weapon.magSize:soldier.weapon.ammo)-1);root.BattleSoldierModel.triggerAnimation(soldier,T.fire,{weapon:soldier.weapon.kind,ammo:soldier.weapon.ammo});}if(oldFire)oldFire.apply(sim,arguments);};return sim;};
  root.BattleModules.registerSystem('soldier-combat-animation',{version:'22-pr',beforeBattleRestart:function(sim){['us','ge'].forEach(function(f){(sim._roster[f]||[]).forEach(function(s){s.reloading=false;s.reloadUntil=0;if(s.weapon)s.weapon.ammo=s.weapon.magSize||s.weapon.stats.magazine||8;});});}});
  console.log('[ANIM] fire/reload event bridge loaded');
})(typeof window!=='undefined'?window:globalThis);
