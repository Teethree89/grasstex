name: Deploy grass demo

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic 50webs deployment disabled. The live loader now pulls the current GitHub game build directly."
